use rx::core::*;

fn update(values: Vec<Box<&mut i32>>) {
    **values[0] = 2;
}
fn main() {
    let mut value = 1;
    let mut values = Vec::<Box<&mut i32>>::new();
    values.push(Box::<&mut i32>::new(&mut value));
    update(values);
}
