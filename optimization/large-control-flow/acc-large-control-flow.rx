32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*18089u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*18123u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*18157u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*18191u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    x
}

fn step_69(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*18079u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*18113u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*18147u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*18181u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*18215u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*18249u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*18283u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*18317u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*18351u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*18385u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*18419u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*18453u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    x
}

fn step_70(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*18341u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*18375u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*18409u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*18443u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*18477u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*18511u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*18545u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*18579u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*18613u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*18647u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*18681u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*18715u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    x
}

fn step_71(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*18603u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*18637u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*18671u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*18705u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*18739u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*18773u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*18807u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*18841u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*18875u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*18909u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*18943u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*18977u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    x
}

fn step_72(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*18865u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*18899u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*18933u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*18967u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*19001u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*19035u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*19069u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*19103u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*19137u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*19171u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*19205u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*19239u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    x
}

fn step_73(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*19127u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*19161u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*19195u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*19229u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*19263u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*19297u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*19331u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*19365u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*19399u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*19433u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*19467u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*19501u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    x
}

fn step_74(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*19389u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*19423u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*19457u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*19491u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*19525u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*19559u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*19593u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*19627u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*19661u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*19695u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*19729u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*19763u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    x
}

fn step_75(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*19651u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*19685u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*19719u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*19753u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*19787u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*19821u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*19855u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*19889u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*19923u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*19957u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*19991u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*20025u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    x
}

fn step_76(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*19913u32 + key; } else { x = (x >> 15u32) ^ (x << 17u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*19947u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*19981u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*20015u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*20049u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*20083u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*20117u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*20151u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*20185u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*20219u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*20253u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*20287u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    x
}

fn step_77(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*20175u32 + key; } else { x = (x >> 16u32) ^ (x << 16u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*20209u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*20243u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*20277u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*20311u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*20345u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*20379u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*20413u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*20447u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*20481u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*20515u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*20549u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    x
}

fn step_78(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*20437u32 + key; } else { x = (x >> 17u32) ^ (x << 15u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*20471u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*20505u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*20539u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*20573u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*20607u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*20641u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*20675u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*20709u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*20743u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*20777u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*20811u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    x
}

fn step_79(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*20699u32 + key; } else { x = (x >> 18u32) ^ (x << 14u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*20733u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*20767u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*20801u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*20835u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*20869u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*20903u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*20937u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*20971u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*21005u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*21039u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*21073u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    x
}

fn step_80(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*20961u32 + key; } else { x = (x >> 19u32) ^ (x << 13u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*20995u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*21029u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*21063u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*21097u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*21131u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*21165u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*21199u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*21233u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*21267u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*21301u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*21335u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    x
}

fn step_81(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*21223u32 + key; } else { x = (x >> 20u32) ^ (x << 12u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*21257u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*21291u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*21325u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*21359u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*21393u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*21427u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*21461u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*21495u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*21529u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*21563u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*21597u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    x
}

fn step_82(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*21485u32 + key; } else { x = (x >> 21u32) ^ (x << 11u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*21519u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*21553u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*21587u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*21621u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*21655u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*21689u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*21723u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*21757u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*21791u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*21825u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*21859u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    x
}

fn step_83(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*21747u32 + key; } else { x = (x >> 22u32) ^ (x << 10u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*21781u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*21815u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*21849u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*21883u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*21917u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*21951u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*21985u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*22019u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*22053u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*22087u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*22121u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    x
}

fn step_84(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*22009u32 + key; } else { x = (x >> 23u32) ^ (x << 9u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*22043u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*22077u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*22111u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*22145u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*22179u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*22213u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*22247u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*22281u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*22315u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*22349u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*22383u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    x
}

fn step_85(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*22271u32 + key; } else { x = (x >> 24u32) ^ (x << 8u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*22305u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*22339u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*22373u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*22407u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*22441u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*22475u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*22509u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*22543u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*22577u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*22611u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*22645u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    x
}

fn step_86(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*22533u32 + key; } else { x = (x >> 25u32) ^ (x << 7u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*22567u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*22601u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*22635u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*22669u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*22703u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*22737u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*22771u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*22805u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*22839u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*22873u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*22907u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    x
}

fn step_87(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*22795u32 + key; } else { x = (x >> 26u32) ^ (x << 6u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*22829u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*22863u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*22897u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*22931u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*22965u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*22999u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*23033u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*23067u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*23101u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*23135u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*23169u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    x
}

fn step_88(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*23057u32 + key; } else { x = (x >> 27u32) ^ (x << 5u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*23091u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*23125u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*23159u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*23193u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*23227u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*23261u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*23295u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*23329u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*23363u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*23397u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*23431u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    x
}

fn step_89(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*23319u32 + key; } else { x = (x >> 28u32) ^ (x << 4u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*23353u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*23387u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*23421u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*23455u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*23489u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*23523u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*23557u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*23591u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*23625u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*23659u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*23693u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    x
}

fn step_90(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*23581u32 + key; } else { x = (x >> 29u32) ^ (x << 3u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*23615u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*23649u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*23683u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*23717u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*23751u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*23785u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*23819u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*23853u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*23887u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*23921u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*23955u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    x
}

fn step_91(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*23843u32 + key; } else { x = (x >> 30u32) ^ (x << 2u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*23877u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*23911u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*23945u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*23979u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*24013u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*24047u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*24081u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*24115u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*24149u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*24183u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*24217u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    x
}

fn step_92(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*24105u32 + key; } else { x = (x >> 31u32) ^ (x << 1u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*24139u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*24173u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*24207u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*24241u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*24275u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*24309u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*24343u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*24377u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*24411u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*24445u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*24479u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    x
}

fn step_93(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*24367u32 + key; } else { x = (x >> 1u32) ^ (x << 31u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*24401u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*24435u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*24469u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*24503u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*24537u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*24571u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*24605u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*24639u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*24673u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*24707u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*24741u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    x
}

fn step_94(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*24629u32 + key; } else { x = (x >> 2u32) ^ (x << 30u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*24663u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*24697u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*24731u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*24765u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*24799u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*24833u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*24867u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*24901u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*24935u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*24969u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*25003u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    x
}

fn step_95(mut x: u32, key: u32) -> u32 {
    if (x & 1u32) == 0u32 { x = x*24891u32 + key; } else { x = (x >> 3u32) ^ (x << 29u32) ^ key; }
    if (x & 2u32) == 0u32 { x = x*24925u32 + key; } else { x = (x >> 4u32) ^ (x << 28u32) ^ key; }
    if (x & 4u32) == 0u32 { x = x*24959u32 + key; } else { x = (x >> 5u32) ^ (x << 27u32) ^ key; }
    if (x & 8u32) == 0u32 { x = x*24993u32 + key; } else { x = (x >> 6u32) ^ (x << 26u32) ^ key; }
    if (x & 16u32) == 0u32 { x = x*25027u32 + key; } else { x = (x >> 7u32) ^ (x << 25u32) ^ key; }
    if (x & 32u32) == 0u32 { x = x*25061u32 + key; } else { x = (x >> 8u32) ^ (x << 24u32) ^ key; }
    if (x & 64u32) == 0u32 { x = x*25095u32 + key; } else { x = (x >> 9u32) ^ (x << 23u32) ^ key; }
    if (x & 128u32) == 0u32 { x = x*25129u32 + key; } else { x = (x >> 10u32) ^ (x << 22u32) ^ key; }
    if (x & 256u32) == 0u32 { x = x*25163u32 + key; } else { x = (x >> 11u32) ^ (x << 21u32) ^ key; }
    if (x & 512u32) == 0u32 { x = x*25197u32 + key; } else { x = (x >> 12u32) ^ (x << 20u32) ^ key; }
    if (x & 1024u32) == 0u32 { x = x*25231u32 + key; } else { x = (x >> 13u32) ^ (x << 19u32) ^ key; }
    if (x & 2048u32) == 0u32 { x = x*25265u32 + key; } else { x = (x >> 14u32) ^ (x << 18u32) ^ key; }
    x
}
fn main() {
let rounds = getInt(); let mut x = getInt() as u32; let key = getInt() as u32; let mut i = 0;
while i < rounds {
x = step_0(x, key + (i as u32));
x = step_1(x, key + (i as u32));
x = step_2(x, key + (i as u32));
x = step_3(x, key + (i as u32));
x = step_4(x, key + (i as u32));
x = step_5(x, key + (i as u32));
x = step_6(x, key + (i as u32));
x = step_7(x, key + (i as u32));
x = step_8(x, key + (i as u32));
x = step_9(x, key + (i as u32));
x = step_10(x, key + (i as u32));
x = step_11(x, key + (i as u32));
x = step_12(x, key + (i as u32));
x = step_13(x, key + (i as u32));
x = step_14(x, key + (i as u32));
x = step_15(x, key + (i as u32));
x = step_16(x, key + (i as u32));
x = step_17(x, key + (i as u32));
x = step_18(x, key + (i as u32));
x = step_19(x, key + (i as u32));
x = step_20(x, key + (i as u32));
x = step_21(x, key + (i as u32));
x = step_22(x, key + (i as u32));
x = step_23(x, key + (i as u32));
x = step_24(x, key + (i as u32));
x = step_25(x, key + (i as u32));
x = step_26(x, key + (i as u32));
x = step_27(x, key + (i as u32));
x = step_28(x, key + (i as u32));
x = step_29(x, key + (i as u32));
x = step_30(x, key + (i as u32));
x = step_31(x, key + (i as u32));
x = step_32(x, key + (i as u32));
x = step_33(x, key + (i as u32));
x = step_34(x, key + (i as u32));
x = step_35(x, key + (i as u32));
x = step_36(x, key + (i as u32));
x = step_37(x, key + (i as u32));
x = step_38(x, key + (i as u32));
x = step_39(x, key + (i as u32));
x = step_40(x, key + (i as u32));
x = step_41(x, key + (i as u32));
x = step_42(x, key + (i as u32));
x = step_43(x, key + (i as u32));
x = step_44(x, key + (i as u32));
x = step_45(x, key + (i as u32));
x = step_46(x, key + (i as u32));
x = step_47(x, key + (i as u32));
x = step_48(x, key + (i as u32));
x = step_49(x, key + (i as u32));
x = step_50(x, key + (i as u32));
x = step_51(x, key + (i as u32));
x = step_52(x, key + (i as u32));
x = step_53(x, key + (i as u32));
x = step_54(x, key + (i as u32));
x = step_55(x, key + (i as u32));
x = step_56(x, key + (i as u32));
x = step_57(x, key + (i as u32));
x = step_58(x, key + (i as u32));
x = step_59(x, key + (i as u32));
x = step_60(x, key + (i as u32));
x = step_61(x, key + (i as u32));
x = step_62(x, key + (i as u32));
x = step_63(x, key + (i as u32));
x = step_64(x, key + (i as u32));
x = step_65(x, key + (i as u32));
x = step_66(x, key + (i as u32));
x = step_67(x, key + (i as u32));
x = step_68(x, key + (i as u32));
x = step_69(x, key + (i as u32));
x = step_70(x, key + (i as u32));
x = step_71(x, key + (i as u32));
x = step_72(x, key + (i as u32));
x = step_73(x, key + (i as u32));
x = step_74(x, key + (i as u32));
x = step_75(x, key + (i as u32));
x = step_76(x, key + (i as u32));
x = step_77(x, key + (i as u32));
x = step_78(x, key + (i as u32));
x = step_79(x, key + (i as u32));
x = step_80(x, key + (i as u32));
x = step_81(x, key + (i as u32));
x = step_82(x, key + (i as u32));
x = step_83(x, key + (i as u32));
x = step_84(x, key + (i as u32));
x = step_85(x, key + (i as u32));
x = step_86(x, key + (i as u32));
x = step_87(x, key + (i as u32));
x = step_88(x, key + (i as u32));
x = step_89(x, key + (i as u32));
x = step_90(x, key + (i as u32));
x = step_91(x, key + (i as u32));
x = step_92(x, key + (i as u32));
x = step_93(x, key + (i as u32));
x = step_94(x, key + (i as u32));
x = step_95(x, key + (i as u32));
i += 1;
}
printlnInt(x as i32);
}
